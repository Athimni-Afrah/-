{% extends 'base.html.twig' %}

{% block title %}New Produit{% endblock %}

{% block body %}
    <h1>Create new Produit</h1>

    {{ include('produits/_form.html.twig') }}


    <a href="{{ path('app_produits_index') }}">back to list</a>
    <ul>
     <a <button class="btn btn-lg btn-primary" type="submit" class="nav-link" href="{{ path("app_accueil") }}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">logout</font></font></a>
        </button>

        </span>
    </li>
    </ul>

{% endblock %}
